/**
 * Bookmarks Feature Module
 *
 * Responsible for managing bookmarks and favorites.
 * Users can bookmark nodes for later reference and favorite nodes
 * for quick access.
 *
 * Structure:
 *   api/        — Bookmark/Favorite API calls
 *   components/ — Bookmark-specific UI (bookmark lists, toggle buttons)
 *   hooks/      — Bookmark-specific React hooks
 *   types/      — Bookmark-specific TypeScript types
 */

export {};
