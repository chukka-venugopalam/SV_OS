/**
 * Progress Feature Module
 *
 * Responsible for tracking learning progress across all knowledge nodes.
 * Includes progress stats, completion tracking, and learning streaks.
 *
 * Structure:
 *   api/        — Progress API calls
 *   components/ — Progress-specific UI (stats cards, progress bars, charts)
 *   hooks/      — Progress-specific React hooks
 *   types/      — Progress-specific TypeScript types
 */

export {};
