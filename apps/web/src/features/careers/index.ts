/**
 * Careers Feature Module
 *
 * Responsible for career exploration and roadmaps.
 * Displays career paths with required/recommended/bonus knowledge nodes.
 *
 * Structure:
 *   api/        — Career API calls
 *   components/ — Career-specific UI (career cards, roadmaps, salary info)
 *   hooks/      — Career-specific React hooks
 *   types/      — Career-specific TypeScript types
 */

export {};
