/**
 * Settings Feature Module
 *
 * Responsible for user profile and application settings.
 * Includes profile editing, password change, preferences, and account management.
 *
 * Structure:
 *   api/        — Settings API calls
 *   components/ — Settings-specific UI (profile forms, preference toggles)
 *   hooks/      — Settings-specific React hooks
 *   types/      — Settings-specific TypeScript types
 */

export {};
