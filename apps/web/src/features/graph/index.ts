/**
 * Graph Feature Module
 *
 * Responsible for the interactive knowledge graph visualization.
 * Uses React Flow for rendering and the graph service for data.
 *
 * Structure:
 *   api/        — Graph API calls (re-exports from services when needed)
 *   components/ — Graph-specific UI components (custom nodes, controls, panels)
 *   hooks/      — Graph-specific React hooks (useGraphViewport, useGraphData)
 *   types/      — Graph-specific TypeScript types
 */

export {};
