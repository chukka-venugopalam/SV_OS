/**
 * Knowledge Feature Module
 *
 * Responsible for browsing and searching knowledge nodes.
 * Includes node detail pages, prerequisites, related nodes, and resources.
 *
 * Structure:
 *   api/        — Knowledge node API calls
 *   components/ — Node-specific UI (node cards, detail panels, resource lists)
 *   hooks/      — Node-specific React hooks
 *   types/      — Node-specific TypeScript types
 */

export {};
